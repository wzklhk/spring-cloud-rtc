server:
  port: 8500

spring:
  # spring.application.name在bootstrap中定义
  #application:
  #  name: access-auth  # 服务名称

  # redis配置从配置中心获取
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    password: password